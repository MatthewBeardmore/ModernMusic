<UserControl
    x:Class="ModernMusic.Controls.NowPlayingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernMusic.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="360"
    d:DesignHeight="614.5"
    xmlns:data="using:ModernMusic.Library"
    Loaded="nowPlayingControl_loaded"
    xmlns:controls="using:ModernMusic.Controls"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    d:DataContext="{Binding Source={d:DesignData Source=../../ModernMusic.Library/Library/Samples/SampleSong.json}}">
    <Grid x:Name="gridBackground" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{ThemeResource PageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="110"/>
            <RowDefinition Height="250*"/>
            <RowDefinition Height="110"/>
            <RowDefinition Height="52"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10,20,0,0" HorizontalAlignment="Left">
            <TextBlock Text="{Binding Song.Artist}" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="NoWrap" FontSize="32"/>
            <TextBlock Text="{Binding Song.Album}" Style="{StaticResource SubheaderTextBlockStyle}" TextWrapping="NoWrap" FontSize="27"/>
        </StackPanel>
        <Grid Grid.Row="1" Grid.Column="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Vertical">
                <SymbolIcon Margin="0,0,0,60" Tapped="repeatButton_Tapped" Symbol="RepeatAll" Width="60" Height="40"/>
                <SymbolIcon Margin="0,0,0,60" Tapped="shuffleButton_Tapped" x:Name="shuffleButton" Symbol="Shuffle" Width="60" Height="40"/>
                <SymbolIcon Margin="0,0,0,0" Tapped="playlistButton_Tapped" x:Name="playlistButton" Symbol="AllApps" Width="60" Height="40"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="0,0,60,0" HorizontalAlignment="Stretch">
                <Image x:Name="AlbumArtwork" Stretch="Uniform">
                    <Image.Source>
                        <BitmapImage x:Name="AlbumArtworkImage" UriSource="ms-appx:///Assets/DarkGray.png"/>
                    </Image.Source>
                </Image>
                <controls:TimeSliderControl Height="33"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" Margin="10,5,0,0" HorizontalAlignment="Left">
            <TextBlock Margin="0,0,0,0" Text="{Binding PreviousSong.SongTitle}" VerticalAlignment="Top" Width="Auto" Style="{StaticResource ListViewItemSubheaderTextBlockStyle}" TextWrapping="NoWrap"/>
            <TextBlock Margin="0,-8,0,0" Text="{Binding Song.SongTitle}" VerticalAlignment="Top" Width="Auto" Style="{StaticResource SubheaderTextBlockStyle}" TextWrapping="NoWrap" FontSize="27"/>
            <TextBlock Margin="0,-2,0,0" Text="{Binding NextSong.SongTitle}" VerticalAlignment="Top" Width="Auto" Style="{StaticResource ListViewItemSubheaderTextBlockStyle}" TextWrapping="NoWrap"/>
            <TextBlock Margin="0,-5,0,0" Text="{Binding SubsequentSong.SongTitle}" VerticalAlignment="Top" Width="Auto" Style="{StaticResource ListViewItemSubheaderTextBlockStyle}" TextWrapping="NoWrap"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="20,0,0,0" HorizontalAlignment="Left" Width="80" Height="60" Tapped="previousButton_Click" PointerPressed="icon_PointerPressed" PointerReleased="icon_PointerCaptureLost" PointerExited="icon_PointerCaptureLost" PointerCaptureLost="icon_PointerCaptureLost">
            <Border BorderThickness="3" BorderBrush="White" CornerRadius="90" Width="45" Height="45">
                <SymbolIcon x:Name="previousButton" Symbol="Previous" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
        <Grid Grid.Row="3" Margin="0,0,0,0" HorizontalAlignment="Center" Width="80" Height="60" Tapped="playButton_Click" PointerPressed="icon_PointerPressed" PointerReleased="icon_PointerCaptureLost" PointerExited="icon_PointerCaptureLost" PointerCaptureLost="icon_PointerCaptureLost">
            <Border BorderThickness="3" BorderBrush="White" CornerRadius="90" Width="45" Height="45">
                <SymbolIcon x:Name="playButton" Symbol="Pause" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
        <Grid Grid.Row="3" Margin="0,0,20,0" HorizontalAlignment="Right" Width="80" Height="60" Tapped="nextButton_Click" PointerPressed="icon_PointerPressed" PointerReleased="icon_PointerCaptureLost" PointerExited="icon_PointerCaptureLost" PointerCaptureLost="icon_PointerCaptureLost">
            <Border BorderThickness="3" BorderBrush="White" CornerRadius="90" Width="45" Height="45">
                <SymbolIcon x:Name="nextButton" Symbol="Next" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
