<Page
    x:Class="ModernMusic.NowPlaying"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernMusic"
    xmlns:controls="using:ModernMusic.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    Background="{ThemeResource PageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <ContinuumNavigationTransitionInfo/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>
    
    <Pivot Title="MODERN MUSIC" Margin="0" CommonNavigationTransitionInfo.IsStaggerElement="True">
        <PivotItem Header="now playing" CommonNavigationTransitionInfo.IsStaggerElement="True">
            <controls:NowPlayingControl x:Name="nowPlayingControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </PivotItem>
    </Pivot>
</Page>
